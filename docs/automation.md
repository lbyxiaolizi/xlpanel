# 自动化与集成

XLPanel 预留了自动化与支付通道的扩展点。

## 自动化

- 使用 `AutomationService` 注册定时任务。
- 可连接任务调度器（Celery、APS、K8s CronJob 等）。

## 支付通道

- 使用 `PaymentsService` 注册支付通道配置。
- 每个通道实现统一的接口（授权、扣款、退款）。

## 事件驱动

建议引入事件总线或消息队列用于以下场景：

- 订单创建 -> 自动开通服务
- 账单逾期 -> 自动暂停
- 支付成功 -> 自动解锁
